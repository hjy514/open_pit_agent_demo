# 露天矿智能装备集群项目技术交接与运行说明

> 当前状态日期：2026-08-29  
> 适用对象：项目开发、测试、演示与材料编制人员  
> 文档用途：统一记录项目目标、已验证能力、运行流程、能力边界和后续任务。

---

## 1. 项目定位

赛题编号：`CS-202616`

赛题名称：

```text
Agent・具身智能集群矿山安全高效成套装备创新应用解决方案
```

项目围绕露天矿边坡失稳风险，构建多装备协同的动态监测、风险预警、任务调度、异常接管和复核反馈闭环。

对外项目名称建议统一为：

**面向露天矿边坡风险的智能体驱动异构装备集群监测预警与协同调度系统。**

项目对应三项核心目标：

1. 固定监测站与移动装备协同感知，支撑广域动态监测。
2. 智能体完成任务规划、候选装备评估、人机协同确认和任务下派。
3. 融合固定站与移动监测数据，完成边坡风险识别、主动预警、安全停车、任务接管和复核反馈。

---

## 2. 项目工程组成

项目由两个代码仓库和 CARLA 仿真环境组成。

```text
矿山调度/
├── open_pit_agent_demo/       # 感知、风险、调度、CARLA适配与API
└── open_pit_dispatch_app/     # PyQt6调度中心
```

运行时实际包含四个部分：

| 组件 | 主要职责 | 运行环境 |
|---|---|---|
| CARLA | 加载矿山地图、矿卡模型、道路与车辆物理仿真 | CARLA 0.9.10 |
| Agent API | 对外提供车辆、任务、监测、风险、命令和摄像头接口 | `openpit-agent` |
| 场景运行器 | 推进仿真 Tick，采集数据，触发边坡事件并执行调度指令 | `openpit-agent` |
| 调度中心 | 展示地图、装备、监测、预警、决策、人工确认和多车视角 | `openpit-ui` |

正式演示以 PyQt6 调度中心为统一操作界面。早期浏览器态势台仅作为历史功能和调试备用，不再作为正式比赛入口。

---

## 3. 当前稳定演示场景

场景名称：

```text
持续强降雨诱发东帮边坡异常及多矿卡动态任务接管演练
```

核心配置：

- CARLA 地图：`0325_5`
- 矿卡蓝图：`vehicle.cat.cat`
- 矿卡数量：3辆
- 固定监测站：8个
- 移动监测装备：3辆矿卡
- 监测区域：4个
- 边坡风险源：东帮边坡2号风险区
- 固定随机种子：`202616`

三车初始任务：

| 车辆 | 初始任务 | 主要能力 |
|---|---|---|
| 矿区巡检矿卡01 | 东帮主运输通道巡检 | 巡检、视觉感知、日常巡查 |
| 边坡复核矿卡02 | H1安全复核点驻守与任务接管待命 | 巡检、视觉感知、激光雷达 |
| 综合巡检矿卡03 | 南部运输节点巡查 | 巡检、应急处置、道路管控 |

完整事件链：

```text
矿卡01、矿卡03执行初始巡检任务，矿卡02在H1安全复核点驻守待命
→ 固定站与移动装备持续回传数据
→ 风险等级按蓝、黄、橙、红递进
→ Tick 520 边坡红色风险触发
→ 矿卡01中止当前任务并安全停车
→ 系统从矿卡02和矿卡03中评估候选车辆
→ 按能力、实时位置、当前负载和距目标距离给出推荐
→ 调度员在调度中心确认方案
→ 接管车辆继续完成矿卡01未完成任务
→ 未被选中的矿卡继续原任务
→ 接管车辆完成接管后恢复自身原任务
→ 复核数据回传，形成闭环记录
```

当前场景不扩大实施区域封锁。系统持续保留已知边坡风险信息，为后续车辆提供风险提示和避险依据。

---

## 4. 已完成并验证的能力

### 4.1 CARLA 仿真与装备执行

- 连接 CARLA 0.9.10 预编译版。
- 加载自定义露天矿地图 `0325_5`。
- 发现或生成3辆 `vehicle.cat.cat` 矿卡。
- 读取车辆位置、速度、健康、任务和路线状态。
- 通过 CARLA `BasicAgent` 执行配置路线。
- 支持任务队列、高优先级接管和原任务恢复。
- 支持暂停、恢复、紧急停车和人工改派。
- 支持车后上方跟车视角、矿山全局视角和三车视角。

### 4.2 监测、风险与闭环

- 8个固定监测站按配置坐标和指标采集数据。
- 3辆矿卡作为移动感知设备回传观测数据。
- 支持位移、位移速率、加速度、降雨量、孔隙水压和裂缝宽度等指标。
- 透明阈值基线完成蓝、黄、橙、红风险评估。
- 任务、命令、执行回执、复核数据和闭环结果可追溯。
- 监测面板实时显示累计观测量、风险等级和闭环阶段。

### 4.3 调度决策与人机协同

- 按装备能力、健康状态、当前负载和目标距离评估候选车辆。
- 展示候选车辆、综合代价和推荐依据。
- 支持调度员接受推荐或选择其他车辆人工改派。
- 任务列表显示原车辆、推荐车辆、目标区域和当前阶段。
- 命令队列显示等待 CARLA 执行、执行中、成功或失败回执。
- 车辆、任务、区域和状态使用统一中文业务名称。

### 4.4 调度中心可视化

- CARLA 道路、当前路线、历史轨迹、任务区和风险区显示。
- 固定监测站以实心点显示，任务区使用颜色区域表示。
- 装备卡片、装备详情和调度文本在定时刷新时保留滚动位置。
- 支持全局监控与3辆矿卡跟车画面。
- 支持监测—预警—调度—执行—复核反馈状态展示。

### 4.5 验证状态

- 61项 Python 自动测试通过。
- 调度中心主窗口、装备管理、人工调度和决策页面离屏启动检查通过。
- 项目组已完成当前版本 CARLA 综合流程运行确认，未发现阻断演示的问题。

---

## 5. 能力边界与对外表述原则

比赛材料、演示解说和对外汇报必须区分“已完成”和“后续工程化”。

### 当前可以展示的能力

- 自定义露天矿地图与矿卡仿真。
- 固定站与移动装备合成数据采集。
- 基于透明阈值的边坡风险递进评估。
- 异常车辆安全停车与未完成任务释放。
- 候选矿卡动态评分、人工确认和任务接管。
- 未选车辆保持原任务，接管车辆后续恢复原任务。
- 调度中心实时状态、命令回执和多视角监控。

### 当前不应宣称已完成的能力

- 当前监测数据和风险阈值是参数化合成 Demo 数据，不是真实矿山测量或正式预警阈值。
- 当前路线是基于配置点和 CARLA `BasicAgent` 的逻辑路线，不是通用路网边级动态规划。
- 当前边坡事件主要由数据、风险区和车辆响应呈现，未实现真实落石、滑塌和扬尘物理特效。
- 尚未接入真实矿山数据、正式矿山风险模型、现场试验和安全认证。
- 尚未实现真实多渠道通知、数据库服务和生产系统对接。
- 当前调度决策以能力、安全、距离和负载约束为主，尚未完成神经网络调度策略训练或在线强化学习。

对外介绍时建议使用“可运行原型”、“仿真验证”、“参数化合成场景”和“人机协同调度”等准确表述。

---

## 6. 核心配置和代码位置

| 路径 | 职责 |
|---|---|
| `configs/mine_competition_demo.json` | 矿山地图、三车、任务区、边坡事件和多摄像头配置 |
| `configs/risk_slope_competition_synthetic.json` | 合成边坡观测序列、阈值和风险等级 |
| `configs/monitoring_demo.json` | 4个监测区和8个固定站配置 |
| `run_demo.py` | Mock/CARLA 场景命令行入口 |
| `src/open_pit_agent/cli.py` | 主运行闭环、风险触发、API同步和命令执行 |
| `src/open_pit_agent/adapters/carla_adapter.py` | CARLA连接、矿卡生成、路线、摄像头和物理控制 |
| `src/open_pit_agent/scheduler.py` | 初始调度、候选评分、任务释放和接管 |
| `src/open_pit_agent/risk.py` | 风险评估规则和边坡事件触发 |
| `src/open_pit_agent/interfaces/api_server.py` | HTTP API、命令队列和摄像头接口 |
| `src/open_pit_agent/runtime_state.py` | 跨进程运行状态 |
| `scripts/validate_hazard_takeover.py` | 边坡接管流程自动验证工具 |
| `open_pit_dispatch_app/main.py` | 调度中心入口（同一仓库的 open_pit_dispatch_app/ 子目录） |
| `open_pit_dispatch_app/windows/human_dispatch_window.py` | 人机协同决策、任务下派和执行回执 |
| `open_pit_dispatch_app/widgets/camera_wall.py` | 全局与三车视频监控 |

---

## 7. 当前运行方式

当前版本使用多进程架构，需要依次启动 CARLA、Agent API、调度中心和场景运行器。项目根目录的启动脚本会自动识别本机常用的`openpit-agent`、`openpit-ui`和CARLA 0.9.10路径，日常启动不需要手动设置环境变量。

### 7.1 启动 CARLA

在项目根目录执行：

```bash
./start_carla.sh
```

### 7.2 启动 Agent API

```bash
cd open_pit_agent_demo
./start_api.sh
```

### 7.3 启动 PyQt6 调度中心

```bash
cd open_pit_agent_demo
./start_dispatch_app.sh
```

### 7.4 启动矿山综合场景

三车边坡Golden Demo继续使用稳定入口：

```bash
cd open_pit_agent_demo
./start_slope_demo.sh
```

S01–S07和S09的6/8车多场景使用统一入口，只需更换场景号：

```bash
./run_scenario.sh --scenario s02 --mode carla --vehicle-count 6 --seed 202601 --ticks 1000
```

支持的CARLA场景为`s01`、`s02`、`s03`、`s04`、`s05`、`s06`、
`s07`和`s09`；S09建议使用`--vehicle-count 8`。先做只读准备检查时加：

```bash
./run_scenario.sh --scenario s02 --mode carla --vehicle-count 6 --seed 202601 --check-only
```

未加`--no-record`时，执行反馈、场景事件、闭环Cycle和任务结果会写入
`data/database/openpit.db`和`artifacts/runs/`。

只检查 CARLA 和 Agent API，不重置状态、不启动场景：

```bash
./start_slope_demo.sh --check-only
```

启动正式场景前，如需检查当前CARLA路网、矿卡蓝图、两个数据库、API和PyQt6环境：

```bash
./check_environment.sh --check-api --check-ui
```

`OPENPIT_PYTHON`、`OPENPIT_DISPATCH_PYTHON`和`OPENPIT_CARLA_ROOT`仍可作为跨电脑时的可选路径覆盖；自动识别不到环境时才需要设置。

CARLA刚启动时通常停在Town03，正式场景才会加载`0325_5`，因此默认检查会提示地图状态但不阻断；在矿图已加载后加`--require-target-map`可将其作为硬性验证。

该脚本会：

1. 检查 Agent Python 环境。
2. 检查 CARLA 连接和 Python API。
3. 检查 Agent API。
4. 清理上一轮 API 状态和遗留指令。
5. 加载 `0325_5` 地图并生成缺失矿卡。
6. 使用固定配置运行边坡失稳与多矿卡任务接管场景。

以下完整命令仅作为调试和故障排查备用：

```bash
cd open_pit_agent_demo
"$OPENPIT_PYTHON" run_demo.py \
  --mode carla-run \
  --config configs/mine_competition_demo.json \
  --risk-config configs/risk_slope_competition_synthetic.json \
  --monitoring-config configs/monitoring_demo.json \
  --load-map \
  --spawn-missing \
  --ticks 30000
```

红色风险触发后，调度员在“人工调度中心”中查看候选车辆评分，点击“按推荐下派接管任务”完成人工确认。

项目当前采用“系统服务与演示场景分开启动”的方式。CARLA、Agent API 和调度中心保持独立，`start_slope_demo.sh` 只负责检查依赖并运行当前稳定场景。这样可以避免为了重新演示场景而频繁重启系统服务。

现有 `start_mine_demo.sh` 主要用于早期 CARLA 与浏览器态势台，不作为当前正式比赛场景入口。

`configs/mine_competition_demo.json` 中的 `carla.root` 仍保留早期验证机器路径。当 CARLA 已由项目人员手动启动时不影响连接；实现一键启动时应改为本地配置或环境变量，不应在公共配置中继续固定个人绝对路径。

---

## 8. 测试与验收

### 8.1 Python 自动测试

```bash
cd open_pit_agent_demo
"$OPENPIT_PYTHON" -m unittest discover -s tests -v
```

当前基线：

```text
Ran 189 tests
OK
```

### 8.2 正式演示验收项

1. 地图为 `0325_5`，3辆矿卡能生成；矿卡01、03执行初始任务，矿卡02在H1安全复核点待命。
2. 调度中心显示8个固定站和3个移动装备。
3. 累计观测数随运行增长，风险等级按配置递进。
4. 矿卡01在红色风险后停车并释放未完成任务。
5. 调度中心显示原车、候选车、推荐车和推荐依据。
6. 调度员确认后，命令状态由等待执行转为成功回执。
7. 接管车辆前往矿卡01原任务区，未选车辆继续原任务。
8. 全局视角和3车视角可正常刷新。
9. 最终显示任务接管完成、复核反馈和闭环完成。

---

## 9. 运行证据与数据管理

每次运行的主要证据保存在：

```text
artifacts/runs/<scenario-id>-<timestamp>-<suffix>/
```

常见文件：

| 文件 | 含义 |
|---|---|
| `scenario_snapshot.json` | 实际场景、种子、环境、任务和事件快照 |
| `events.jsonl` | 运行事件和状态变化 |
| `summary.json` | 任务、车辆、风险、工单和终态指标 |
| `agent_decisions.jsonl` | 感知、风险、调度和执行决策记录 |
| `experience_dataset.jsonl` | 状态、行动、结果和奖励样本 |
| `acceptance_report.json` | 自动功能验收结果 |

以下内容不应提交到代码仓库：

- `artifacts/runs/` 每次演示的运行产物。
- `artifacts/control/` 进程日志。
- `artifacts/live_cameras/` 实时摄像头帧和清单。
- 真实矿山数据、企业凭据、个人路径和未授权素材。

需要用于比赛材料的证据应由项目组拷贝到独立的受控材料目录，不与实时运行文件混合。

---

## 10. 已知问题和后续任务

### P0：比赛 Demo 收尾

1. 完成 `start_slope_demo.sh` 的一次正式 CARLA 启动验收。
2. 录制3至5分钟稳定演示视频。
3. 保存矿山地图、固定站、风险预警、接管方案、人工下派、多车视角和闭环完成截图。
4. 整理测试报告、使用说明和作品设计报告。
5. 每次正式演示前运行自动测试和完整验收清单。

### P1：可选视觉增强

- 增加边坡落石、滑塌或扬尘视觉效果。
- 增加响应时间、任务完成率、接管成功率等演示指标。

### P2：后续工程化

- 接入经授权的真实或典型矿山监测数据。
- 结合矿区规范和专家意见重新标定风险指标与阈值。
- 实现真实路网边级动态避险和多车时空冲突消解。
- 接入生产数据库、工单系统和多渠道通知服务。
- 开展算法基线对比、工业现场试验和安全评估。

---

## 11. 项目维护规范

1. 任何能力都必须区分“已通过 CARLA 验证”、“仅通过自动测试”和“规划中”。
2. 合成数据必须保留 `SYNTHETIC_DEMO_DATA_NOT_REAL_MINE_MEASUREMENTS` 标记。
3. 不得将配置点分段导航表述为通用道路边级动态绕行。
4. 调度业务保持为 CARLA 外部第三方系统，不写入 UE4/CARLA 源码。
5. 保持 Python 3.7 和 CARLA 0.9.10 兼容性。
6. 修改车辆、路线、任务和风险规则后，必须先运行完整自动测试，再执行相应 CARLA 验收；测试数量以实际测试输出为准。
7. 正式演示分支不随意重做界面布局或更换可视化技术路线。
8. 不向 Git 提交实时摄像头帧、运行日志、个人凭据和未授权数据。
9. 重要改动完成后，同步更新本文档的“已验证能力”、“能力边界”、“测试状态”和“变更记录”。

---

## 12. 变更记录

### 2026-08-01

- 完成 Town03 阶段的车辆仿真、任务调度、故障接管、风险评估、工单闭环和早期可视化。
- 完成风险解释、处置建议、经验数据和自动验收基线。

### 2026-08-28

- 接入自定义露天矿地图 `0325_5` 和 `vehicle.cat.cat` 矿卡模型。
- 增加8个固定监测站、3个移动监测设备和实时安全闭环。
- 增加边坡异常触发、受影响车辆停车和候选车辆接管逻辑。
- 增加调度中心全局与多车视角。

### 2026-08-29

- 完善矿卡01受影响、矿卡02/矿卡03动态候选、人工确认和原任务恢复流程。
- 调整场景为保留风险信息而不扩大实施区域封锁。
- 优化调度中心车辆名、任务名、决策方案、任务下派、执行回执和滚动体验。
- 完成61项自动测试和当前版本 CARLA 综合演示确认。
- 将项目文档统一为团队技术交接口径。
- 新增 `start_slope_demo.sh`，统一检查 CARLA 和 Agent API 后启动边坡失稳与任务接管场景。

---

## 13. 跨电脑接续与仓库规范

项目根目录为 `/home/xiaoa/矿山调度/open_pit_agent_demo`，远程仓库为
`https://github.com/hjy514/open_pit_agent_demo.git`。新电脑接续前先执行：

```bash
git fetch origin
git status
git pull --ff-only origin main
```

Python 后端优先使用 `openpit-agent` 环境；PyQt 调度中心优先使用
`openpit-ui` 环境或 `open_pit_dispatch_app/.venv`。CARLA 固定使用 0.9.10 和
`0325_5` 地图，正式启动不附加低画质或离屏参数。

跨电脑后应先执行以下基础验证，再运行任何场景：

```bash
./check_environment.sh --require-target-map
/home/xiaoa/miniconda3/envs/openpit-agent/bin/python -m unittest discover -s tests -q
```

提交规则：源码、配置、测试和正式文档可以提交；`artifacts/`、
`data/database/`、本地日志、摄像头帧、个人环境路径和凭据不得提交。Git 负责保存
历史版本，不在工作树中保留 `old`、`backup`、`final` 等副本文件。
